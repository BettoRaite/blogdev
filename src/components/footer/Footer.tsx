import styles from "./footer.module.css";

function Footer() {
	return (
		<footer className={styles.footer}>
			I know I need to add something here. It makes sense.
		</footer>
	);
}
export default Footer;
